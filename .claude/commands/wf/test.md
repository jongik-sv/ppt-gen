---
subagent:
  primary: quality-engineer
  description: TDD/E2E 테스트 실행 및 품질 검증
mcp-servers: [context7, playwright]
hierarchy-input: true
parallel-processing: true
---

# /wf:test - 테스트 실행 (Lite)

> **상태 변경 없음**: 반복 실행 가능
> **적용 category**: development, defect, infrastructure
> **실행 조건**: 구현 완료 상태 이후
> **계층 입력**: WP/ACT/Task 단위 (하위 Task 병렬 처리)

## 사용법

```bash
/wf:test [PROJECT/]<WP-ID | ACT-ID | Task-ID> [--type tdd|e2e|all]
```

| 예시 | 설명 |
|------|------|
| `/wf:test TSK-01-01` | TDD + E2E 모두 |
| `/wf:test TSK-01-01 --type tdd` | TDD만 |
| `/wf:test TSK-01-01 --type e2e` | E2E만 |
| `/wf:test ACT-01-01` | ACT 내 모든 구현완료 Task 병렬 |

---

## 실행 가능 상태

| category | 실행 가능 상태 |
|----------|---------------|
| development | `[im]`, `[vf]`, `[xx]` |
| defect | `[im]`, `[vf]`, `[xx]` |
| infrastructure | `[im]`, `[vf]`, `[xx]` |

---

## 테스트 유형

| 유형 | 대상 | 실행 단계 |
|------|------|----------|
| TDD | Backend 단위/통합 | 1 → 2 → 4 |
| E2E | Frontend 시나리오 | 1 → 3 → 4 |
| all | TDD + E2E | 1 → 2 → 3 → 4 |

---

## 실행 과정

### 0단계: 사전 검증 ⭐

명령어 실행 전 상태 검증:

```bash
npx tsx .orchay/script/transition.ts {Task-ID} test -p {project} --start
```

| 결과 | 처리 |
|------|------|
| `canTransition: true` | 다음 단계 진행 |
| `canTransition: false` | 에러 출력 후 즉시 종료 |

**에러 출력:**
```
[ERROR] 현재 상태 [{currentStatus}]에서 'test' 액션을 실행할 수 없습니다.
필요한 상태: [im], [vf], [xx]
```

### 1단계: 테스트 환경 준비

```
문서 로드:
├── 026-test-specification.md (테스트 명세) ⭐
│   ├── 단위 테스트 시나리오 (UT-XXX)
│   ├── E2E 테스트 시나리오 (E2E-XXX)
│   ├── 테스트 데이터 (Fixture)
│   └── data-testid 목록
└── 025-traceability-matrix.md (요구사항 매핑)
```

### 2단계: TDD 테스트 실행

**테스트-수정 루프** (최대 5회) ⭐:

```
🔄 TDD 테스트-수정 루프:
├── 1️⃣ 단위 테스트 실행
├── 2️⃣ 실패 분석
│   ├── AssertionError → 비즈니스 로직 수정
│   ├── TypeError → 타입/null 체크
│   └── Mock 실패 → Mock 설정 수정
├── 3️⃣ 코드 수정
├── 4️⃣ 문서 동기화 (코드 변경 시) ⭐
│   ├── 020-detail-design.md 업데이트
│   └── 030-implementation.md 업데이트
└── 5️⃣ 재실행 (최대 5회)
```

### 3단계: E2E 테스트 실행

> ⚠️ **서버 재사용 규칙**: E2E 테스트 실행 전 기존 개발 서버가 실행 중인지 확인하세요.
> - 실행 중이면 새로 실행하지 말고 기존 서버 사용
> - 서버가 없을 때만 새로 시작 (`npm run dev` 등)
> - 테스트 완료 후 서버 종료하지 않음

**테스트-수정 루프** (최대 5회) ⭐:

```
🔄 E2E 테스트-수정 루프:
├── 1️⃣ E2E 테스트 도구 실행
├── 2️⃣ 실패 분석
│   ├── Locator 실패 → data-testid 확인
│   ├── Timeout → waitFor 추가
│   └── Network 에러 → API 연동 수정
├── 3️⃣ 코드 수정
├── 4️⃣ 문서 동기화 (코드 변경 시) ⭐
│   ├── 011-ui-design.md 업데이트
│   └── 030-implementation.md 업데이트
└── 5️⃣ 재실행 (최대 5회)
```

### 4단계: 결과 문서 생성

> ⚠️ **MUST**: 이 단계를 완료하지 않으면 완료 신호를 출력하지 마세요.

**1. 타임스탬프 디렉토리 생성** (형식: `YYYYMMDD-HHMMSS`):

```bash
# 타임스탬프 생성 및 디렉토리 구조 생성
TIMESTAMP=$(date +%Y%m%d-%H%M%S)
mkdir -p test-results/${TIMESTAMP}/tdd/coverage
mkdir -p test-results/${TIMESTAMP}/e2e/screenshots
```

**2. 테스트 결과 파일 생성**:

| 파일 | 경로 | 필수 |
|------|------|------|
| TDD 결과서 | `{Task폴더}/070-tdd-test-results.md` | ✅ |
| E2E 결과서 | `{Task폴더}/070-e2e-test-results.md` | ✅ |
| 커버리지 리포트 | `test-results/{timestamp}/tdd/coverage/` | ✅ |
| E2E HTML 리포트 | `test-results/{timestamp}/e2e/e2e-test-report.html` | ✅ |
| 스크린샷 | `test-results/{timestamp}/e2e/screenshots/` | ⚠️ (실패 시) |

**3. 검증** (완료 신호 출력 전 필수 확인):

```bash
# 폴더 존재 및 파일 생성 확인
ls -la test-results/${TIMESTAMP}/
ls -la test-results/${TIMESTAMP}/tdd/coverage/
ls -la test-results/${TIMESTAMP}/e2e/
```

**4. WBS 업데이트**: `test-result: none` → `pass` | `fail`

---

## 품질 기준

| 항목 | 기준 |
|------|------|
| TDD 커버리지 | 80% 이상 |
| TDD 통과율 | 100% |
| E2E 통과율 | 100% |

---

## 출력 예시

```
[wf:test] 테스트 실행

Task: TSK-01-01-01 | 유형: all

🧪 TDD:
├── 🔄 루프: 2회 시도
│   ├── 1차: 2/12 실패 → Service 수정
│   │   └── 📝 020-detail-design.md 동기화
│   └── 2차: 12/12 ✅
└── 커버리지: 85%

🎭 E2E:
├── 🔄 루프: 3회 시도
│   ├── 1차: 3/8 실패 → Locator 수정
│   ├── 2차: 1/8 실패 → waitFor 추가
│   └── 3차: 8/8 ✅
└── 스크린샷: 8장

📝 문서 동기화: 2개 문서 업데이트
📋 WBS: test-result → pass

---
ORCHAY_DONE:{project}/TSK-01-01-01:test:success
```

---

## 에러 케이스

| 에러 | 메시지 |
|------|--------|
| 상태 미달 | `[ERROR] [im] 이전 상태입니다. 구현 완료 필요` |
| 테스트 명세 없음 | `[ERROR] 026-test-specification.md가 없습니다` |
| TDD 5회 초과 | `[ERROR] TDD 5회 시도 후 실패. 수동 개입 필요` |
| E2E 5회 초과 | `[ERROR] E2E 5회 시도 후 실패. 수동 개입 필요` |
| 커버리지 미달 | `[WARN] 커버리지 미달: N% (목표: 80%)` |

---

## 완료 신호

작업의 **모든 출력이 끝난 후 가장 마지막에** 다음 순서로 실행:

**1. execution 필드 제거:**
```bash
npx tsx .orchay/script/transition.ts {task-id} -p {project} --end
```

**2. 완료 신호 출력:**

**성공:**
```
ORCHAY_DONE:{project}/{task-id}:test:success
```

**실패:**
```
ORCHAY_DONE:{project}/{task-id}:test:error:{에러 요약}
```

> ⚠️ 이 출력은 orchay 스케줄러가 작업 완료를 감지하는 데 사용됩니다. 반드시 정확한 형식으로 출력하세요.

---

## 공통 모듈 참조

@.claude/includes/wf-common-lite.md
@.claude/includes/wf-auto-commit-lite.md

---

<!--
wf:test lite
Version: 1.1
-->
